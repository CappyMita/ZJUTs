"""
RAG 系统配置文件
===============
集中管理所有可调参数，方便实验和调优。
"""

import os

# ============================================================
# HuggingFace 镜像配置（国内用户建议使用镜像加速）
# ============================================================
# 设置 HuggingFace 镜像端点，解决国内无法访问 huggingface.co 的问题
# 如果不需要镜像，将此行注释掉即可
os.environ["HF_ENDPOINT"] = "https://hf-mirror.com"

# ============================================================
# DeepSeek API 配置
# ============================================================
DEEPSEEK_API_KEY = "我删了"
DEEPSEEK_BASE_URL = "https://api.deepseek.com"
DEEPSEEK_CHAT_MODEL = "deepseek-chat"  # DeepSeek-V3

# ============================================================
# 本地模型配置（无需 API，本地运行）
# ============================================================
# Embedding 模型：将文本转为向量
# bge-small-zh-v1.5 是中文专用的轻量模型，效果好、速度快
EMBEDDING_MODEL_NAME = "BAAI/bge-small-zh-v1.5"

# Reranker 模型：对召回结果进行重排序
# bge-reranker-v2-m3 支持多语言，中文效果优秀
RERANKER_MODEL_NAME = "BAAI/bge-reranker-v2-m3"

# ============================================================
# 文档切分配置
# ============================================================
CHUNK_SIZE = 500        # 每个 chunk 的最大字符数
CHUNK_OVERLAP = 50      # chunk 之间的重叠字符数（防止语义断裂）

# ============================================================
# 检索配置（召回 + 重排 两阶段）
# ============================================================
RECALL_TOP_K = 20       # 召回阶段：从向量库中取多少个候选
RERANK_TOP_N = 5        # 重排阶段：最终保留多少个最相关的片段

# ============================================================
# 路径配置
# ============================================================
BASE_DIR = os.path.dirname(os.path.abspath(__file__))
DATA_DIR = os.path.join(BASE_DIR, "data")
VECTORDB_DIR = os.path.join(BASE_DIR, "vectordb")

# 项目中的攻略文件路径
DATA_FILE = os.path.join(DATA_DIR, "guide.md")
